//submit all forms, create post option with 3GoodThings and Photo.
import React from "react";



const ProfileDashboard: React.FC = () => {
//if user is logged in, and has completed the journal entries of the day, show profile dashboard with user info and journal entries

//else if user is logged in, and has not completed the journal entries of the day, show profile dashboard with user info and a message to complete the journal entries, with option to complete later but view social media feed 

    return (
      <div>
        {/* Placeholder content - you can leave this empty for now */}
      </div>
    );
  };
  
  export default ProfileDashboard;